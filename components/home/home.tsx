import Link from 'next/link';
import styles from './home.module.scss'

const Home: React.FC = () => {
  return (
    <div className={styles.mainWrapper}>
      something
    </div>
  );
};

export default Home;
